<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | callEDGE</title>
    <link rel="stylesheet" href="css/base.css">
    <link rel="stylesheet" href="css/home.css">
</head>
<body>

    <nav>
        <p style="font-family: var(--font4) ;">callEDGE</p>
        <a href="/logout"><i class="material-icons md-48">logout</i></a>
    
    </nav>
    

    <div class="container">
        <div class="hero-main">
            <div class="first">Hello <%= user %></div>
            <div class="second">I don't know what to write so I wrote this.</div>
            <div class="meeting-actions">
                <div onclick="window.location = 'room/'" class="create-meet">
                    <span class="material-icons">videocam</span>Create new meet
                </div>
                <div class="input-box">
                    <span class="material-icons">keyboard</span><input placeholder="Enter meet link" id="meeting-link" class="meeting-link"></input>
                    <button id="join" class="join" onclick="window.location = 'room/' + document.getElementById('meeting-link').value"><span class="material-icons">chevron_right</span></button>
                </div>
            </div>
        </div>
        <div class="hero-image">
            <img src="images/hero.png"
        </div>
    </div>
</body>
<script>
    var join = document.getElementById('join')
    var meetLink = document.getElementById('meeting-link') 
    meetLink.addEventListener('onfocus', () => {
        if(meetLink.value.length == 0){
            join.style.color = "gray";
        }
        else{
            join.style.color = "var(--primary)";
        }

    })

    
</script>

</html>